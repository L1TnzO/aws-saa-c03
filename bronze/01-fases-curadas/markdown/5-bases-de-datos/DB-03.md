> **BRONZE Markdown** — `DB-03` · Servicio: **Amazon RDS** · Fase 5 · Dominios examen: D2, D3
> URL oficial: https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_ReadRepl.html
> Título oficial: Working with DB instance read replicas

A *read replica* is a read-only copy of a DB instance. You can reduce the
load on your primary DB instance by routing queries from your applications to the read replica. In
this way, you can elastically scale out beyond the capacity constraints of a single DB instance
for read-heavy database workloads.

To create a read replica from a source DB instance, Amazon RDS uses the built-in replication features
of the DB engine. For information about using read replicas with a specific engine, see the
following sections:

- Working with replicas for Amazon RDS for Db2 (./db2-replication.html)
- Working with MariaDB read replicas (./USER_MariaDB.Replication.ReadReplicas.html)
- Working with read replicas for Microsoft SQL Server in Amazon RDS (./SQLServer.ReadReplicas.html)
- Working with MySQL read replicas (./USER_MySQL.Replication.ReadReplicas.html)
- Working with read replicas for Amazon RDS for Oracle (./oracle-read-replicas.html)
- Working with read replicas for Amazon RDS for PostgreSQL (./USER_PostgreSQL.Replication.ReadReplicas.html)

After you create a read replica from a source DB instance, the source becomes the primary DB instance.
When you make updates to the primary DB instance, Amazon RDS copies them asynchronously to the read
replica. The following diagram shows a source DB instance replicating to a read replica in a
different Availability Zone (AZ). Clients have read/write access to the primary DB instance and
read-only access to the replica.

- ### On this page

Overview (#USER_ReadRepl.Overview)
- ### Related resources

Amazon RDS API Reference (https://docs.aws.amazon.com/AmazonRDS/latest/APIReference/index.html)AWS CLI commands for Amazon RDS (https://docs.aws.amazon.com/cli/latest/reference/rds/)SDKs & Tools  (https://aws.amazon.com/tools/)

#### Next topic:

Differences between DB
engines

#### Previous topic:

Renaming a DB instance
